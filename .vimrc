syntax enable
set tabstop=2 "visual spaces per tab
set softtabstop=2 "spaces in tab when editign
set expandtab "tabs are spaces
set number "line numbers
set showcmd "show command in bottom bar

filetype indent on "load file specific indent

set wildmenu "visual complete for command menu

set lazyredraw "only redraw when required

set showmatch "highlight matching brackets, parent
